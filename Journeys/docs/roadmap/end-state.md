# End state

A multi-tenant program runtime: customer events in → journey + rules in the middle → typed outcomes out. Agent-assisted authoring uses the same APIs as production execution. Loyalty is the proof; the pattern is general.
