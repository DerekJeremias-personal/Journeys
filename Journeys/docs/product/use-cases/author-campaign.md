# Use case: author a campaign

**Persona:** `campaign-author`  
**Realized by:** `campaign-agent`, `mcp-api`  
**Implemented as:** `Journeys.API` (CampaignAgent, MCP), `Journeys.Agent`

Configure event models, account types, journeys, and campaigns against the live API contract. The agent must not invent a parallel write path that skips Core services.
