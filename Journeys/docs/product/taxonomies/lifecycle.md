# Taxonomy: lifecycle

| Id | Meaning |
|----|---------|
| `draft` | Mutable program state; default authoring surface |
| `live` | Executable production program; gated promotion |
| `extend` | Change an existing graph node / capability |
| `invent` | Add a new capability node (requires spec + graph update) |
