# Ontology: draft vs live

Programs are versioned **Draft** (authoring, mutation) and **Live** (execution). Prefer draft-first mutation where the product already requires it. Promoting to live is a gated, explicit operation — not a side effect of a chat turn.
