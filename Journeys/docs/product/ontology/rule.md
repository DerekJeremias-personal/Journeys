# Ontology: rule

A **rule** is a declarative condition/action evaluated by the polymorphic rules engine in `Journeys.Core`. Rules are part of executable campaigns, not ad-hoc `if` statements in `Journeys.API`.
