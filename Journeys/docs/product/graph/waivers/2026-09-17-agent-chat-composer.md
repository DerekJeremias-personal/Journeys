# Waiver: Agent chat composer layout and Enter-to-send

**Reason:** The unlabeled Agent page already exists. This change stacks **Send** under the textarea, submits on Enter (Shift+Enter = newline), and documents that turns persist in the existing Backend `AgentMessage` container. No new capability, ontology term, or graph edge.

**Nodes touched by path-map (no meaning change):** `campaigns`, `campaign-agent`.
