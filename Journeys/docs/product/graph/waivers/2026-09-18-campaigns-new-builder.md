# Waiver: Campaigns IA New Journey Builder

**Reason:** Task 5 replaces the focused `/loyalty/campaigns/new` stub with the EXP progressive Journey Builder plus unlabeled `AgentChat` rail. Hydrate still GET Draft and keep-local/apply-Agent when dirty. Save uses existing `updateCampaign`. No new capability, ontology term, or graph edge.

**Nodes touched by path-map (no meaning change):** `campaigns`, `campaign-agent`.
