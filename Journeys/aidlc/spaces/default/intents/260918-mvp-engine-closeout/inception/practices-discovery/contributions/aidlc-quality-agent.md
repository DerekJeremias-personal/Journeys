**Collaborator:** aidlc-quality-agent

## Contribution

Quality review of the Practices Discovery draft for intent `mvp-engine-closeout`. Scope is testing posture, coverage tooling, in-tree quality gates, and test/code patterns. No product code. No durable ALWAYS/NEVER invented.

**In-tree quality gate (affirmed, keep).** Construction does not exit green without `scripts/aidlc-agent-verify-sensor.ps1` (wrapper over `scripts/agent-verify.ps1` plus docs-impact and graph-impact). Fail closed. `docs/developer/testing.md` is consistent: `dotnet test .\Journeys.Tests\Journeys.Tests.csproj` is the suite command; `agent-verify.ps1` runs that suite when a changed path is under `Journeys.Tests\` **or** `-RunTests` is set. Draft Ordering correctly adds `-RunTests` when tests changed. That is the only evidenced quality gate. There is no `.github/` workflow and no `azure-pipelines.yml` in this tree, so there is no in-repo CI coverage, mutation, or flake gate to cite. Do not draft a second CI quality bar for this increment.

**Coverage tooling (inferred, do not promote).** CodeKB and the draft agree: xUnit 2.9.3 + coverlet.collector 6.0.3; no `coverlet.runsettings`; no coverage-floor file. Org classic 80% line-coverage is not evidenced and must not be treated as an in-repo gate. Enforcing an unmeasured floor on a brownfield suite with known closeout holes (`NotificationOutcomeTests`, `JourneyNodeCollectRulesTests`, child-node hydrate, `LoyaltyAccountExpirationCascadeTests`, `EventServiceBringPointsCurrentTests`) would invent a failing criterion the existing suite was never held to. Interview must choose: keep “suite green + verify script,” or affirm a floor **and** name how it is measured. Until then, coverlet stays a collector, not a gate.

**Methodology vs history.** Draft Methodology is `tdd`. That matches the approved closeout plan (failing tests first on collect-rules, cascade clock, expire-before-rules, NotificationOutcome Calculate/Award) and `.cursor/rules/tests.mdc` (“Prefer TDD on pure seams … when the plan says so”). Historical `Journeys.Tests` layout looks test-after. Quality does not treat that history as permission to skip red-first on these seams. Durable Methodology may stay `tdd` (new seams) or become `custom` (mixed cadence). Interview must pick one; do not leave Methodology as an unlabeled `tdd` that later stages will read as team-wide always-TDD.

**Test placement and style (affirmed).** One project: `Journeys.Tests`. Follow existing area folders (RulesEngine/Outcomes, Services, Mcp, …). Arrange–Act–Assert. Do not add a second test project. UX Vitest is out of increment. No BDD feature files — do not introduce them. `Journeys.Tests` must not reference `Journeys.Notification`; fake `INotificationService` on `RulesEngineState`. Prefer the same state-injection style already used for `loyaltyAccountService`. Adding `INotificationService` to the `RulesService` constructor will break long explicit constructors in `RuleServiceTests` and is the wrong seam for a notification fake.

**Closeout suite gaps the interview should not paper over (construction facts, not new rules).** Existing cases lock the wrong thing relative to this increment: `ExpirePointsOutcomeTests` locks balances, not dest ExpirationDate / EarnDate+365; `RuleServiceTests` locks root HistoricalRule TTL/decay, not child-node hydrate; `UserPointsTests` locks service calls, not ProcessEvent order. PointLifecycle cases that assume UtcNow-reset must be rewritten to the earn-date formula — that is an affirmed plan task, not optional cleanup. `TestDataFactory.GetSpendablePointAccount()` uses a 30-day Spendable lifespan while the stated program example is 365; lifecycle assertions must set 365 or assert EarnDate + configured days. `PointAccountTypeValidatorTests` is already sufficient for the G3 pairing validator. `RulesEngineMcpContractSummaryTests` pins `MatrixVersion` `"2026-06-20"` and must move with any matrix bump.

**Verify-path detection risk (interview).** Path-triggered test runs miss a Core-only edit if the red test is not in the same changed-file set. For this intent, Ordering should require `-RunTests` on every Construction verify that touches engine seams (hydrate, cascade clock, expire-on-process, `NotificationOutcome`), not only when a `Journeys.Tests\` path happens to appear. That is stricter than the current script default and needs a spoken yes/no.

**Docs as a quality sensor, not a second suite.** Verify already includes docs-impact / graph-impact. CodeKB records live drift (`process-event.md` already claims notifications fire; `path-map.yaml` still marks `Journeys.Notification` `meaningOptional: true`; loyalty-account / rule docs omit earn-date cascade and tree hydrate). Those are construction/doc-sync obligations when the matching unit ships, not a reason to add a new testing methodology.

**Out of quality scope (leave to other spokes / interview).** Feat-branch lifetime, walking-skeleton ceremony, and where staging/prod CD lives are not test-posture items. SAST / secret / dependency scanning is not evidenced as a test gate; quality does not invent one.

## Positions

- AGREE: Construction quality gate stays `scripts/aidlc-agent-verify-sensor.ps1`, fail closed. No in-tree CI quality gate exists; do not invent one this increment.
- AGREE: Closeout units write the failing `Journeys.Tests` case first (collect-rules, cascade clock, expire-before-rules, NotificationOutcome Calculate/Award), then implement until that case and the existing suite pass.
- AGREE: Keep tests in `Journeys.Tests` only; existing folder layout; Arrange–Act–Assert; no second test project; no BDD; UX Vitest out of increment.
- AGREE: Do not add a `Journeys.Notification` test project reference. Fake `INotificationService` on `RulesEngineState`, same injection style as `loyaltyAccountService`. Do not extend the `RulesService` constructor for that fake.
- AGREE: Do not enforce org classic 80% line-coverage. Coverlet.collector is present without a floor; suite-green + verify is the evidenced bar unless the interview affirms a measured floor.
- AGREE: Rewrite PointLifecycle cases that assume UtcNow-reset to the earn-date formula. Do not treat those existing greens as still-valid locks.
- AGREE: Increment-only non-goals (hosted sweep, `/points/expire`, UX product tests) stay out of discovered-rules and out of this increment’s required suite.
- OBJECT: Affirming durable Methodology as `tdd` without the interview resolving historical test-after vs closeout red-first. Increment Ordering can stay TDD; the Methodology field must be `tdd` or `custom` by spoken choice, not by draft default.
- OBJECT: Adopting an 80% (or any) coverage floor, `coverlet.runsettings`, or a new CI job as a closeout gate without an in-repo measurement and a human yes.
- OBJECT: Treating `agent-verify.ps1` path detection (`Journeys.Tests\` dirty) as sufficient for engine-only Construction verifies. Interview must say whether this intent always passes `-RunTests` on those seams.
- OBJECT: Using `TestDataFactory.GetSpendablePointAccount()` 30-day lifespan as the implicit lock for earn-date + 365 cascade tests. Tests must set 365 or assert configured days.
- OBJECT: Counting current `ExpirePointsOutcomeTests` / root-only `RuleServiceTests` / `UserPointsTests` as coverage of dest ExpirationDate, child-node hydrate, or ProcessEvent bring-current order. Those cases do not lock the closeout seams.
