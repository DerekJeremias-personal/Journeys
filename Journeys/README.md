# Journeys

.NET 8 onion solution for the Journeys program runtime (`Journeys.sln`).

**Agents:** start at [AGENTS.md](AGENTS.md). That is the session contract (reading order, path rules, docs/graph impact, definition of done).

**Humans:** same door. Product meaning is `docs/product/`. How the system is built is `docs/platform/`.
